const somatorio = (x: number, y: number): number => {
  return x + y;
}

const somatorioTres = (x: number, y: number, z: number): number => {
  return x + y + z;
}

console.log('hello world');
console.log(somatorio(1, 4))
console.log(somatorioTres(1, 2, 5))